<h1>New current</h1>

<% form_for(@current) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :resolved_url %><br />
    <%= f.text_field :resolved_url %>
  </p>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', currents_path %>