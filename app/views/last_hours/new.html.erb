<h1>New last_hour</h1>

<% form_for(@last_hour) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :resolved_url %><br />
    <%= f.text_field :resolved_url %>
  </p>
  <p>
    <%= f.label :count %><br />
    <%= f.text_field :count %>
  </p>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', last_hours_path %>