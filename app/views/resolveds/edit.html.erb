<h1>Editing resolved</h1>

<% form_for(@resolved) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :resolved_url %><br />
    <%= f.text_field :resolved_url %>
  </p>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @resolved %> |
<%= link_to 'Back', resolveds_path %>